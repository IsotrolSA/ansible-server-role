---
  - name: Test and restart SSHD
    command: sshd -t
    notify: ["Restart SSHD"]
    tags: sshd

  - name: Restart SSHD
    service:
      name: "ssh"
      state: "restarted"
    tags: sshd

  - name: Start SSHD
    service:
      name: "ssh"
      state: "started"
    tags: sshd
